[Unit]
Description=AWS Flask App
After=network.target

[Service]
User=ec2-user
Group=ec2-user
WorkingDirectory=/opt/app
Environment="PATH=/opt/app/venv/bin"
ExecStart=/opt/app/venv/bin/flask --app app run --host=0.0.0.0 --port=5000
Restart=on-failure

[Install]
WantedBy=multi-user.target
